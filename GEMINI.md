# Project Context: collector

## Overview
This directory is currently empty and appears to be the starting point for a new project named `collector`. No existing code or documentation was found during the initial analysis.

## Getting Started
Since this is a fresh workspace, consider the following steps to initialize your project:

1.  **Initialize Version Control:**
    ```bash
    git init
    ```

2.  **Initialize Project Structure:**
    *   **For Node.js:** `npm init -y`
    *   **For Python:** `python3 -m venv venv` and create a `requirements.txt`
    *   **For Go:** `go mod init <module-name>`
    *   **For Rust:** `cargo init`

## Development Conventions
*   **Documentation:** Maintain this `GEMINI.md` file as the project evolves to provide context for AI agents and new developers.
*   **Structure:** Follow standard directory structures for your chosen language/framework.

## Directory Overview
*   `GEMINI.md`: This file, serving as the project's contextual documentation.

# CLIProxy Configuration - Production Example
# Copy to .env and update with your actual values

# CLIProxy Backend URL
# For Docker: use host.docker.internal to access host machine
# For remote: use actual hostname/IP
CLIPROXY_URL=http://host.docker.internal:8317

# CLIProxy Management API Key
# IMPORTANT: Keep this secret! Do not commit to git
CLIPROXY_MANAGEMENT_KEY=your-production-management-key-here

# Collector Configuration
# Interval between automatic collections (in seconds)
# 300 = 5 minutes (recommended for production)
COLLECTOR_INTERVAL_SECONDS=300

# HTTP API port for manual triggers and health checks
COLLECTOR_TRIGGER_PORT=5001

# Timezone Configuration
# Offset in hours from UTC (e.g., 7 for UTC+7, -5 for UTC-5)
TIMEZONE_OFFSET_HOURS=7

# Database Configuration
# For Docker: MUST be /app/data/collector.db (mapped to ./data/ on host)
# For local: can be any path like ./data/collector.db
DB_PATH=/app/data/collector.db

# Optional: Management session timeout (default: 1 hour)
# MANAGEMENT_SESSION_TIMEOUT_MS=3600000
